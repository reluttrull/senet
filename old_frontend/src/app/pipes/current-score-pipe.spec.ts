import { CurrentScorePipe } from './current-score-pipe';

describe('CurrentScorePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentScorePipe();
    expect(pipe).toBeTruthy();
  });
});
